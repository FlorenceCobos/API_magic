 CREATION MACHINE VIRTUELLE
cd Desktop mkdir VirtualBox cd VirtualBox vagrant init * mkdir data nano Vagrantfile (retirer les commentaires) ( config.vm.box = "ubuntu/xenial64" ) ( config.vm.network "private_network", ip: "192.168.33.10" ) ( config.vm.synced_folder "./data", "/var/www/html" ) vagrant up
vagrant ssh  
sudo apt update 
sudo apt install apache2 php7.0 -y
 
sudo nano /etc/php/7.0/apache2/php.ini
Ensuite, CTRL + W pour chercher "display_errors" Le faire jusqu'à tomber sur :  - display_errors : Off  - display_startup_errors : Off Passer les deux sur On Et redémarrer le serveur Apache

sudo service apache2 restart

sudo apt install mysql-server -y

   sudo apt install php7.0-mysql
sudo
  sudo nano /etc/apache2/envvars

(modifier les lignes :export APACHE_RUN_USER=vagrant
                                           export APACHE_RUN_GROUP=vagrant )
          en mettant vagrant
pour installer Adminer; https://github.com/vrana/adminer/releases/download/v4.6.3/adminer-4.6.3.php
                                    aller dans /var/www/html, puis avec la commande wget on peut installer directement via des liens
 
renomer en : mv adminer-4.6.3.php adminer.php
taper mtn: http://192.168.33.10/adminer.php pour y accéder

                                    pour se connecter a mysql: mysql -uroot -p
               pour sortir : exit
                         
vagrant halt en dehors de la vagrant pour l’éteindre           

si probleme 500 ou autre essayer:
sudo cat /var/log/apache2/error.log